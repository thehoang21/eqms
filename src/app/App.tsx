import React from 'react';
import { AppRoutes } from './routes';

const App: React.FC = () => {
  return (
    <React.StrictMode>
      <AppRoutes />
    </React.StrictMode>
  );
};

export default App;
