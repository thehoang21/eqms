import React from 'react';
import { AppRoutes } from './MainLayout/AppRoutes';

const App: React.FC = () => {
  return (
    <React.StrictMode>
      <AppRoutes />
    </React.StrictMode>
  );
};

export default App;
